name: github pages

on:
  push:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@main
        with:
          submodules: true
      
      - name: build and deploy
        uses: benmatselby/hugo-deploy-gh-pages@main
        env:
          CNAME: mihaiplesa.com
          HUGO_EXTENDED: true
          TARGET_REPO: mihaiplesa/mihaiplesa.com
          TOKEN: ${{ secrets.PERSONAL_TOKEN }}
